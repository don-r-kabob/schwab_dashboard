
streamlit:
  refreshtimer: 300
  pagelayout: wide
  layout: default #valid options: default; 131;
schwab:
  configfile: schwab.app_config.json
  tokenfile: schwab.token_file.json

app:
  version: 0.1.5
  aws: true
  gcp: false

useaws: true
usegcp: false

aws:
  s3bucket: name
  dynamodb:
    table: schwab_api_auth_token
    primary_key: authtoken
